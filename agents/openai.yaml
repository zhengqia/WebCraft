interface:
  display_name: "WebCraft"
  short_description: "Build secure VicroCode apps with hosted API cloning"
  default_prompt: "Use $webcraft to build or adapt this VicroCode app, including runtime, storage, Credential Vault proxying, secret removal, clone readiness, testing, and publishing."
